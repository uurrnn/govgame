import { h } from "preact";

import CharacterCreation from "../CharacterCreation/CharacterCreation";

const Game = () => (
    <main className="game">
        <CharacterCreation />
    </main>
);

export default Game;
